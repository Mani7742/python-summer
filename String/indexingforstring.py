# indexing for string
str = "Ammara Akhtar"
print(len(str)) # len() is a function used to get the length of the string
ch = str[1]
print(ch)
print(str[5])

#str[2] = a --------> this thing is not allowed in python, we cannot maniputale the values 